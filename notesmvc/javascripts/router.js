App.Router.map(function() {
    this.route('notes', {
        path: '/notes'
    });

    this.route('bookmarks', {
        path: '/bookmarks'
    });

    this.route('tasks', {
        path: 'tasks'
    });
});